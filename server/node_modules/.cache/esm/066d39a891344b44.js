let formatErrors;_913‍.x([["default",()=>_913‍.o]]);_913‍.w("../formatErrors",[["default",["formatErrors"],function(v){formatErrors=v}]]);

_913‍.d({
  Query: {
    allTeams: (parent, args, { models }) => models.Team.findAll(),
  },
  Mutation: {
    createTeam: async (parent, args, {models, user}) => {
      try {
        await models.Team.create({...args, owner: user.id});
        return {
          ok: true,
        }
      } catch (err) {
        _913‍.g.console.log(err)
        return {
          ok: false,
          errors: formatErrors(err)
        };
      }
    }
  }
});
