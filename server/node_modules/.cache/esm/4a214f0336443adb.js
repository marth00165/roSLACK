let express,ApolloServer,gql,makeExecutableSchema,path,fileLoader,mergeTypes,mergeResolvers,cors,models,jwt,refreshTokens,execute,subscribe,createServer,SubscriptionServer;_364‍.w("express",[["default",["express"],function(v){express=v}]]);_364‍.w("apollo-server-express",[["ApolloServer",["ApolloServer"],function(v){ApolloServer=v}],["gql",["gql"],function(v){gql=v}]]);_364‍.w("graphql-tools",[["makeExecutableSchema",["makeExecutableSchema"],function(v){makeExecutableSchema=v}]]);_364‍.w("path",[["default",["path"],function(v){path=v}]]);_364‍.w("merge-graphql-schemas",[["fileLoader",["fileLoader"],function(v){fileLoader=v}],["mergeTypes",["mergeTypes"],function(v){mergeTypes=v}],["mergeResolvers",["mergeResolvers"],function(v){mergeResolvers=v}]]);_364‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_364‍.w("./models",[["default",["models"],function(v){models=v}]]);_364‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_364‍.w("./auth",[["refreshTokens",["refreshTokens"],function(v){refreshTokens=v}]]);_364‍.w("graphql",[["execute",["execute"],function(v){execute=v}],["subscribe",["subscribe"],function(v){subscribe=v}]]);_364‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);_364‍.w("subscriptions-transport-ws",[["SubscriptionServer",["SubscriptionServer"],function(v){SubscriptionServer=v}]]);












const types = fileLoader(path.join(__dirname, './schema'));
const resolversArray = fileLoader(path.join(__dirname, './resolvers'));

const SECRET = 'madaraUchihaWasInnocent';
const SECRET2 = 'El_curry'

const typeDefs = mergeTypes(types);



const resolvers = mergeResolvers(resolversArray);




const PORT = 8081;
const app = express();
app.use(cors('*'))

const addUser = async (req, res, next) => {
  const token = req.headers['x-token'];
  if (token) {
    try {
      const { user } = jwt.verify(token, SECRET);
      req.user = user;
    } catch (err) {
      const refreshToken = req.headers['x-refresh-token'];
      const newTokens = await refreshTokens(token, refreshToken, models, SECRET, SECRET2);
      if (newTokens.token && newTokens.refreshToken) {
        res.set('Access-Control-Expose-Headers', 'x-token, x-refresh-token');
        res.set('x-token', newTokens.token);
        res.set('x-refresh-token', newTokens.refreshToken);
      }
      req.user = newTokens.user;
    }
  }
  next();
};

app.use(addUser);





const schema = makeExecutableSchema({
  typeDefs,
  resolvers,
});

const graphqlEndpoint = '/graphql';

const server = new ApolloServer({
  schema,
  context:({req}) => {
  return {
      user: req.user,
     SECRET,
     SECRET2,
    models,
  }
}

});
server.applyMiddleware({ app });

const wsServer = createServer(app)

models.sequelize.sync({}).then(() => {
  wsServer.listen(8081, ()=>{
    new SubscriptionServer({
      execute,
      subscribe,
      schema
    },
      {
        server,
        path: '/subscriptions'
      },
    );
  });
});
